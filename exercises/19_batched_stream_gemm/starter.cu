#include "cuda_utils.cuh"
#include <iostream>

// Exercise: Concurrent Batched GEMM
// Objective: Launch independent tiled matrix multiplies across multiple streams.
// TODO: implement the CUDA kernel(s), host memory management, launch, and validation.
// Reference solution: ../../solutions/19_batched_stream_gemm.cu

int main() {
  std::cout << "TODO: solve Concurrent Batched GEMM\n";
  return 0;
}
